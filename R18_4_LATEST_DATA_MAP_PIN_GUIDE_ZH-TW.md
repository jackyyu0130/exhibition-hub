# R18.4 最新資料、地圖圖釘與單選分類更新指南

## 這次更新

- 以受保護的更新流程重新抓取文化部分類與已啟用官方來源；過期或異常資料未通過品質門檻時會保留上一版，不會用空資料覆蓋官網。
- 本次手動快照已完成：資料時間為 **2026-09-14 02:08（台灣時間）**；公開清單為 **733 筆展覽、108 處展演場館**。相較前一版移除 539 筆已過期活動，仍在舉辦或日期不明的移除限制為 25 筆，本次實際移除 19 筆，並保留完整更新報告於 `data/update-reports/`。
- `Weekly exhibition update (low resource)` 仍保留每週日排程：台灣時間週日 03:17。只有把 repository variable `WEEKLY_UPDATE_ENABLED` 設為 `false` 才會暫停。
- 附近展場地圖可直接點擊任意位置插入搜尋圖釘，依該點 3 公里內的已定位展場重新排序；點擊既有展場圖釘仍可切換搜尋中心。
- 探索展覽的分類改為單選：點另一個分類會取代上一個分類，再點目前分類可清除選擇；舊的多分類網址也只保留第一個有效分類。

本次抓取中有兩個文化部分類端點回應空資料，另有兩個官方場館端點反應過慢；流程因此保留上一版該來源資料，沒有用空資料覆蓋。下一次週日排程仍會再嘗試，其他成功來源與文化部最新資料已寫入本快照。

## 手動更新最新資料

1. 到 GitHub repository 的 **Actions**。
2. 選 **Weekly exhibition update (low resource)**。
3. 按右上角 **Run workflow**。
4. `Use workflow from` 選 `main`。
5. `confirm` 輸入完全一致的 `RUN_WEEKLY_UPDATE`，再按 **Run workflow**。
6. 等待所有步驟變成綠色；摘要會列出更新前後筆數、新增、異動、移除、官方來源成功／失敗數。
7. 成功後重新整理 `https://twexhibition.com/`。資料日期顯示的是資料快照的 `updatedAt`，不是 Actions 部署時間。

若手動更新失敗，先不要連續重跑；請查看第一個紅色步驟。流程設有備份與品質門檻，失敗時會還原正式資料。

若某個官方場館端點卡住，可在本機重跑時先暫時設定 `EXHIBITION_HUB_EXCLUDE_SOURCES=來源ID1,來源ID2`；只會跳過該來源並保留它的上一版資料，不會影響其他來源。下一次週日排程會照常重新嘗試。

## 附近展場：插入圖釘

1. 開啟「附近展場」，允許瀏覽器取得目前位置。
2. 地圖上橘色圓點是目前位置；虛線圓是 3 公里範圍。
3. 直接點擊地圖任意空白位置，會插入「地圖選取位置」圖釘，右側清單改列該點 3 公里內展場。
4. 點擊已存在的展場圖釘，也會以該展場為中心重新搜尋。
5. 按「回到目前位置」可清除圖釘並恢復以定位位置搜尋。

清單只會顯示場館登錄資料且有有效座標的展場；若顯示 0 處，代表該 3 公里範圍目前沒有符合條件的可定位展場。拖曳地圖或放大縮小不會改變搜尋中心，必須點擊地圖。

## 探索展覽：單選分類

- 桌面版與手機版共用同一個單選狀態。
- 點「動漫」後再點「美術」，只會留下「美術」；不會再同時套用兩個分類。
- 再點目前已選分類即可取消分類篩選。

## GitHub Desktop 套用（若使用 ZIP）

將 ZIP 解壓縮後的**內容**複製到：

`/Users/jacky_yu/Documents/GitHub/exhibition-hub`

確認不是多套一層資料夾，也不要覆蓋專案的 `.git`。在 GitHub Desktop 的 `develop` 分支確認變更後 Commit、Push，再建立 `develop → main` PR；合併後等待驗證與 Pages 發布成功。
