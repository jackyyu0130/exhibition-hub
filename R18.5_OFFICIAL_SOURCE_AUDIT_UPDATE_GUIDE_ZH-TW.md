# R18.5 官方來源資料更新包

這是針對華山、松山、駁二與臺南市美術館官方來源比對後的**最小必要更新包**。
它不是整個網站替換包，也不會直接把舊的 `data/exhibitions*.json` 覆蓋掉。

## 這版修正

1. 華山改抓官方目前的 `/exhibition` 雙頁展覽清單，不再只抓舊的 `CustomEvent` 清單。
2. 華山、松山、駁二、臺南市美術館各自最多抓 40 個詳情頁；其他來源維持工作流程的 20 筆低資源預設值。
3. 每週日排程仍保留；失敗時仍保留上一版正式資料，不會用空資料覆蓋網站。
4. 只在官方詳情頁具備標題、日期、圖片與官方網址時發布，避免把半筆資料直接放上前台。

## 要放入專案的檔案

請把 ZIP 內的檔案放到本機專案：

`/Users/jacky_yu/Documents/GitHub/exhibition-hub/`

只替換以下檔案：

- `.github/workflows/update-exhibitions.yml`
- `data/source_registry.json`
- `scripts/exhibition_hub/collectors/huashan.py`
- `scripts/run_collectors.py`
- `scripts/run_official_source_batch.py`
- `tests/test_r185_official_source_audit.py`（測試檔，可一併放入）

不要刪除或整批覆蓋 `data/exhibitions.json`、`data/exhibitions.enriched.json`、
`data/exhibitions.curated.json`、`data/geocode-cache.json`。

## GitHub Desktop 操作

1. 開啟 GitHub Desktop，確認 Current repository 是 `exhibition-hub`。
2. Branch 切到 `develop`。
3. 將上述檔案依相同資料夾路徑拖入專案；若詢問是否取代，只取代同名檔案。
4. 左側 Changes 應只看到上述 5～6 個檔案。
5. Summary 輸入：`fix: repair official source coverage R18.5`。
6. 點 **Commit to develop**，再點 **Push origin**。
7. 到 GitHub 的 Actions，先確認 **Validate development changes** 成功。
8. 建立 `develop → main` Pull Request；確認檢查通過後再合併。
9. 合併後到 Actions → **Weekly exhibition update (low resource)** → **Run workflow**。
   若畫面要求輸入確認文字，輸入：`RUN_WEEKLY_UPDATE`。
10. 等待更新工作流程與 `pages-build-deployment` 都顯示綠色勾勾，再開啟 `https://twexhibition.com/`。

## 更新後檢查

- 首頁的資料更新時間已變成這次執行時間。
- 華山官方清單中的活動不再只剩舊 `CustomEvent` 清單。
- 松山不再因全域 20 筆限制而直接漏掉第 21 筆以後的活動。
- Actions 摘要中的 `official-source-batch.json` 顯示 Songshan、Huashan、Pier-2、Tainan 的 `publishableRecordCount`。
- 若某個來源失敗，應看到「保留上一版資料」，而不是整站變成空白。

## 回復方式

若更新後出現錯誤，不要再整個資料夾取代。回到 GitHub Desktop，選擇 R18.5 的 commit，
點 **Repository → Revert changes in commit**，再 Commit/Push；或只把上面 5 個檔案還原成上一版。

