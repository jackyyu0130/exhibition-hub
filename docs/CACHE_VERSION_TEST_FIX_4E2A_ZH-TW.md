# 4-E-2A｜修正前端快取版本測試

PR 目前失敗不是網站功能壞掉，而是舊測試把前端版本固定寫成
`v=5.0`，但 `index.html` 已更新為 `v=5.2`。

本修正將測試改為：

- 確認 CSS 有版本參數
- 確認 JavaScript 有版本參數
- 確認 CSS 與 JavaScript 使用相同版本
- 不再把版本固定死在 `5.0`

這樣未來更新 `v=5.3`、`v=5.4` 時，不會再次因單純清除快取而失敗。
